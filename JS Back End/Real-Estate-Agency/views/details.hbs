{{#with housing}}
<section id="deatils-page">
    <div class="wrapper">
        <div class="product-img">
            <img src="{{housingImg}}">
        </div>
        <div class="product-info">
            <div class="product-text">
                <div class="product-text">
                    <h1>Name: {{name}}</h1>
                    <h2>Type: {{type}}</h2>
                    <h4>Year: {{year}}</h4>
                    <h4>City: {{city}}</h4>
                    <p>Description: {{description}}</p>
                    
                    <p>
                    {{#if rentorsList}}
                    People rented this housing: {{rentorsList}}
                    {{else}}
                    People rented this housing: There are no tenants yet.
                    {{/if}}
                    </p>
                    
                </div>
            </div>

            {{#if hasUser}}
            <!-- if there is no registered user, do not display buttons-->
            <div class="product-btn">
                {{#if isOwner}}
                <!-- Only for registered user and creator of the housing offer-->
                <a href="/edit/{{_id}}" class="edit">Edit</a>
                <a href="/delete/{{_id}}" class="remove">Delete</a>
                {{else}}
                <!-- logged in user with available pieces-->
                    {{#if hasRented}}
                    <p class="alRentHome">You have already rent this home</p>
                    {{else}}
                        {{#if remainingSpaces}}
                        <a href="/rent/{{_id}}" class="rentHome">Rent a home, available {{remainingSpaces}} housing</a>
                        {{else}}
                        <!-- logged in user with no available pieces-->
                        <p class="no-housing">No Housing Available!</p>
                        {{/if}}
                    {{/if}}
                {{/if}}
            </div>
            {{/if}}
        </div>
    </div>
</section>
{{/with}}